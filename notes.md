- `cargo new hello` will create a new `hello` project with `Git` repository initialize.

- `cargo new hello --vcs none` avoid the creation of the Git repo.

- `cargo doc --open` command will build documentation provided by all the dependencies locally